# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Environment files — NEVER commit real values ─────────────────
# Local dev secrets
.env.local
.env.development.local
.env.test.local
.env.production.local
# Root .env files with real values
.env
.env.apis
# AI backend secrets
ai-backend/.env
ai-backend/.env.local
ai-backend/.env.production
# SAFE to commit (no real values, just templates):
# .env.production.example  ← commit this
# ai-backend/.env.example  ← commit this

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# ── ChromaDB vector store (generated at runtime, large) ──────────
ai-backend/vector_db/chroma_store/

# ── Uploaded files ────────────────────────────────────────────────
ai-backend/uploads/

# ── Curriculum PDFs (large binary files — upload directly to server) ─────────
# These are ingested at runtime into ChromaDB. Keep them on the server only.
ai-backend/curriculum_docs/exam_papers/
ai-backend/curriculum_docs/syllabi/
ai-backend/curriculum_docs/lesson_plans/
ai-backend/curriculum_docs/Scheme/

# ── SQLite local database ─────────────────────────────────────────
*.db
*.db-journal
prisma/dev.db
.env*
